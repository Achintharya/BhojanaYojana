feat: Implement Phase 2 - Pantry & Grocery Intelligence

Complete implementation of pantry management and smart grocery list with
automatic low-stock detection and synchronization.

Features:
- Full pantry CRUD operations with quantity tracking
- Low-stock threshold detection (deterministic)
- Automatic grocery list synchronization
- Expiry date tracking with color-coded states (expired/expiring/normal)
- Filter pantry by: All / Low Stock / Expiring Soon
- Older-item-first support (sorted by expiry date)
- Smart grocery list with auto-generated and manual sections
- Purchase tracking with checkboxes
- Protected auto-generated items (cannot delete until stock restored)

Implementation:
- Business logic layer: pantryLogic.ts with pure, deterministic functions
- Reusable UI components: PantryItemCard, GroceryItemCard
- Modal forms: AddPantryItemModal, AddGroceryItemModal
- Full screen implementations: pantry.tsx, grocery.tsx
- Enhanced data access: getExpiredItems, getGroceryItemByPantryId, etc.

Technical:
- TypeScript strict mode (no errors)
- Clean separation: UI → Logic → Data → SQLite
- No schema changes required
- ~1,430 lines of new production code
- Tablet-optimized UI (48dp+ touch targets)

Integration:
- Pantry changes automatically sync to grocery list
- Low-stock items create grocery entries
- Quantity increases remove auto-generated entries
- No duplicate entries created
- Manual items preserved during sync

V1 Scope Compliance:
- No barcode scanning, cloud sync, backend, AI, or extra features
- Stayed strictly within Phase 2 requirements

Files: 5 new, 4 modified
Database: No schema changes
Tests: All 10 verification cases passed
